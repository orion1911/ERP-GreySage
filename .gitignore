backend/node_modules
backend/BRD.md

# Environment files — ALL variants, in every location. The previous list named
# specific filenames, so frontend/.env.production slipped through and was committed
# with a live MUI X licence key.
.env
.env.*
!.env.example
backend/.env*
frontend/.env*

# MongoDB replica-set keyfile. docker-compose generates its own at container start
# (openssl rand), so this should never be in the tree.
keyfile.txt
*.key
*.pem

bkp

docs

frontend/bkp
frontend/node_modules

frontendv2

build

.env
*.zip
.env.development
.claude/settings.local.json
mongo_init

z_run_coder.bat
